name: qa-gen
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - -e .
      # optional: local semantic search (pip install "sentence-transformers>=2.7,<3")
      # - sentence-transformers>=2.7,<3
